# Screenshots

Place 1280×720 PNGs here matching the filenames referenced in the root `README.md`:

- `homepage.png` — landing page with car listings
- `car-detail.png` — car detail / view page
- `login.png` — login screen

Keep each file ≤ 300 KB (use `pngquant` or `squoosh.app`).
